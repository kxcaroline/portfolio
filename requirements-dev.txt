# Development and CI dependencies.
#
# Pillow builds the thumbnails and is used by the test that checks the
# declared image dimensions against the files on disk. It is deliberately
# absent from requirements.txt: the running site reads those sizes from
# data/thumb_sizes.json, so no image library ships to production.

-r requirements.txt

pytest==8.3.4
pytest-cov==6.0.0
Pillow==11.1.0
pylint==3.3.3
mypy==1.14.1

# Minifies the stylesheet and script into .min siblings (build_assets.py).
rcssmin==1.2.1
rjsmin==1.2.4
